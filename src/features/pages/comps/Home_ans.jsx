import React from "react";

// {Object.keys(dat.answers).map((item) => (
//     <li>
//         <span>{dat.answers[item].answer}</span>
//     </li>
// ))}

const Home_ans = (data) => {

    console.log(data)
    return(
        <span>
            <h1>
            {/* {data && data.map((da) => 
                <p>{da}</p> 

                
            )} */}
            </h1>
        </span>
    )
}

export default Home_ans;